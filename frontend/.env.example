# Base URL for API requests (must include /api)
# Example for same domain with proxy: leave as /api
# Example for separate backend domain: https://api.yourdomain.com/api
VITE_API_URL=/api

# Socket.IO server URL (no trailing slash)
# Example: https://api.yourdomain.com
VITE_SOCKET_URL=
